**2026-08-24: read `CLAUDE.md` in this folder first.** Mission changed — full scope now, full autonomy, no more waiting on Akul's approval except for one thing (new paid infra). This README's task list below is now historical record, not a gate.

**SESSION STATUS (2026-08-22, overnight run): the full queue below is now DONE — 01, 02, 03 (built directly by Akul), 04, 06, 07, 08, 09 all complete, pushed to `main`, and confirmed live 2026-08-24 (see `summary.md`'s deploy-gotcha entry).**

# Abdullah's task queue (2026-08-16, updated 2026-08-22)

**Rule for this file, going forward: only put a task here once any decision it needed has already been made.** If a task still needs Akul to pick a methodology, an approach, or a scope, it does NOT belong in this file — it stays on Akul's own plate until he decides, and only lands here after.

Everything below is cleared to work on independently — none need Akul's approval or a question back to him. Full background/context lives in `docs/plans/2026-08-15-001-feat-sitrep-rebrand-depaywall-plan.md` if you want more; each file here is self-contained enough to start from directly.

Do NOT touch: Railway seed-infra (news feed digest, telegram-feed, gpsjam, oref-alerts) — that's explicitly deferred pending Akul's own decision (cost/billing risk he's weighing), not yours to provision or propose.

Suggested order (not mandatory — pick whatever's most useful):

1. [`01-byok-panels.md`](./01-byok-panels.md) — done
2. [`02-canada-roads-layer.md`](./02-canada-roads-layer.md) — done
3. [`03-resilience-score.md`](./03-resilience-score.md) — done, built directly, nothing left here
4. [`08-server-entitlement-stripping.md`](./08-server-entitlement-stripping.md) — done 2026-08-22, server-side entitlement enforcement stripped fork-wide
5. [`04-sanctions-pressure.md`](./04-sanctions-pressure.md) — done 2026-08-22, OFAC proxy + parse, folded into `api/misc-gateway`
6. [`06-trade-tariffs-debt.md`](./06-trade-tariffs-debt.md) — done 2026-08-22 (debt sub-metric was already done; trade flows + tariffs turned out to need no new data source, just removing 3 redundant client-side gates left over from before 08 — see the file for the full trace)
7. [`07-verify-depaywall-end-to-end.md`](./07-verify-depaywall-end-to-end.md) — re-verified 2026-08-22 after 04/06/08 landed. Flags `stock-backtest`, `global-procurement`, `wsb-ticker-scanner` as likely candidates for the exact same fix `06` just did for `trade-policy` — not fixed (out of this session's scope), a real finding for a future task if Akul wants them unlocked too.
8. [`09-theme-css-tuning.md`](./09-theme-css-tuning.md) — done 2026-08-22, fixed 4 real contrast bugs the SITREP accent-color rebrand introduced (DEFCON badge + pills, BYOK key inputs, desktop settings placeholder)
9. [`10-remaining-panel-unlocks.md`](./10-remaining-panel-unlocks.md) — done 2026-08-23, unlocked `stock-backtest`, `daily-market-brief`, `wsb-ticker-scanner`, `global-procurement` (client-side gates for all 4 + a real server-side tier gate for 2 of them); `latest-brief` confirmed still genuinely Clerk-bound, left locked
10. [`11-map-renderer-boot-race.md`](./11-map-renderer-boot-race.md) — done 2026-08-24, not from the paywall hunt list: fixed the 2D map falling back to plain SVG in local dev (window-width mobile check was a one-shot read at construction time, before the window settled)
11. [`12-supply-chain-trade-unlock.md`](./12-supply-chain-trade-unlock.md) — done 2026-08-24, unlocked supply-chain (8 RPCs), trade (comtrade + tariffs), and food-stocks — confirmed these don't need Railway (scenario modeling and sanctions do, left alone). Full remaining plan at `/Users/muhammadabdullah/.claude/plans/glimmering-sleeping-lerdorf.md`
12. [`13-dead-billing-ui-and-settings-gates.md`](./13-dead-billing-ui-and-settings-gates.md) — done 2026-08-24, deleted ProBanner/CommunityWidget (dead components + ~260 lines of orphaned CSS), removed UnifiedSettings' entire "Plan & billing" tab, unlocked API-keys/MCP-clients settings tabs (`hasFeature()` neutered). Fixed 5 test files broken by the deletions + a source-attribution manifest build failure.
13. [`14-obsolete-paywall-test-cleanup.md`](./14-obsolete-paywall-test-cleanup.md) — done 2026-08-24, rewrote the 12 pre-existing obsolete-by-design test failures across `entitlement-check.test.ts`/`summarize-article-handler-security.test.ts`/`widget-agent-billing-denial.test.ts` to assert the real post-task-08 contract instead of dead denial responses. Found but left alone: 2 more files (`widget-agent-auth.test.mts`, `widget-builder.test.mjs`) with the same stale `api/widget-agent.ts` import bug — pre-existing, unrelated to this mission
14. [`15-scenario-modeling-unlock.md`](./15-scenario-modeling-unlock.md) — done 2026-08-24, unlocked scenario modeling. Chose GitHub Actions (`*/5 * * * *`, matching this repo's already-proven `analytics-collector-monitor.yml`/`seed-freshness-monitor.yml` pattern) over Railway to avoid the recurring cost — `scripts/scenario-worker.mjs` rewritten from an infinite loop to drain-and-exit, new `.github/workflows/scenario-worker-drain.yml`. Also found and fixed a task-12 miss in `SupplyChainPanel.ts`'s own separate bypass-corridors lock. Closes out the full plan (tasks 12-15)
15. [`16-resilience-layer-and-route-explorer-unlock.md`](./16-resilience-layer-and-route-explorer-unlock.md) — done 2026-08-24, two findings never traced by any prior task: dropped the dead `'locked'` marker on the `resilienceScore` map layer (`map-layer-definitions.ts`, parallel to task 03's already-free panel gate) and stripped RouteExplorer's client-side `hasPremiumAccess()` PRO gate + "Upgrade to PRO" CTA (`RouteExplorer.ts`, `LeftRail.ts`, `route-explorer.css`) — the RPC itself was already free server-side via task 12.
16. [`17-anonymous-follow-limit.md`](./17-anonymous-follow-limit.md) — done 2026-08-24, removed the anonymous-user `FREE_TIER_FOLLOW_LIMIT` cap in `followed-countries.ts`'s `addCountry()` (client-only soft gate, zero server backing — anon users have no Convex `entitlements` row to check against) and gated `follow-button.ts`'s "Upgrade to follow more" tooltip on a new `hasSignedInUser()` check so it never lies to anon users. Left `convex/followedCountries.ts`'s signed-in server-side cap fully untouched — genuinely Dodo-billing-backed, same class as `latest-brief`.
17. [`18-cosmetic-pro-badge-cleanup.md`](./18-cosmetic-pro-badge-cleanup.md) — done 2026-08-24, removed dead "Upgrade to API Starter" branch in `UnifiedSettings.ts` (unreachable since `hasFeature()` was neutered in task 13) and the stale `PRO` badge on the widget-builder CTA in `panel-layout.ts` (never actually gated). Both cosmetic-only, no logic touched.
18. [`19-export-gate-and-tab-cap-unlock.md`](./19-export-gate-and-tab-cap-unlock.md) — done 2026-08-24, unlocked `src/services/gates/export-resolver.ts`'s data-export gate and dashboard-tab cap — the last confirmed-LIVE billing gate found this session (`/api/product-catalog` genuinely serves a purchasable "Pro Business" tier, verified via live `curl`, not assumed). Rewrote `tests/tab-cap.test.mts`/`tests/export-gate.test.mts`'s now-dead per-input decision-chain tests down to small "always uncapped"/"always unlocked" blocks, kept their still-real drift-guard/wiring/catalog-probe blocks untouched, deleted `tests/dom/gate-reader-forwarding.test.mts` (zero remaining signal once the resolver ignores its inputs). 3 other flagged files confirmed never actually affected (inject synthetic verdicts, never call the real resolver). `tests/country-evidence-bundle-export.test.mts`'s 3 failures confirmed pre-existing via `git stash` — unrelated, left for a future task.
19. [`20-stale-panel-pro-badge.md`](./20-stale-panel-pro-badge.md) — done 2026-08-24, found while reviewing task 16's report: 4 panels task 10 already fully unlocked (`DailyMarketBriefPanel`, `GlobalProcurementPanel`, `StockBacktestPanel`, `WsbTickerScannerPanel`) still passed `premium: 'locked'` to their `Panel` constructor — a separate, header-badge-only gate task 10 never touched, so a misleading "PRO" badge kept rendering on fully-free panels. Removed from those 4; left untouched on the 4 still-genuinely-BYOK-gated components (`ChatAnalystPanel`, `MarketImplicationsPanel`, `RegionalIntelligenceBoard`, `StockAnalysisPanel`) where the badge is correct.
20. [`21-deferred-not-safe-swaps.md`](./21-deferred-not-safe-swaps.md) — Akul's find, 2026-08-25. Cataloged what has no free/safe path from the paid-service audit (AviationStack, CorridorRisk, DATABASE_URL/Firecrawl, ScrapeCreators/Reddit, SearXNG hosting, `lint:api-contract` CI debt). Mostly superseded by your own 2026-08-25/26 session — see `summary.md`'s Task 1-5 list, you independently re-ran the same audit and shipped real fixes for several of these.
21. [`22-github-actions-minutes-exhausted.md`](./22-github-actions-minutes-exhausted.md) — **PARTIAL, done 2026-08-26.** Akul out of GitHub Actions minutes (6 cron workflows, 1-min billing floor, ~28,800 min/mo). Cut cron intervals on all 5 adjustable workflows tonight (`scenario-worker-drain` 5→15min, `analytics-collector`/`postmerge-deploy` 5,10→30min, `seed-freshness`/`umami-storage` 15min→hourly), `deploy-gate` left at 30min (safety net). ~28,800→~8,640 min/mo, a 70% cut, but still 3-4x over Free/Pro. Full fix (external cron for `scenario-worker-drain`) needs a new public Vercel endpoint (not built) + an external cron-job.org account (needs Akul — account creation isn't something done on his behalf). See file for the full breakdown.

22. [`23-api-contract-lint-stale-check.md`](./23-api-contract-lint-stale-check.md) — done 2026-08-27, `lint:api-contract`'s 26-service "no HTTP gateway" flag was a false positive left over from the dispatcher consolidation, not dead code (verified all 35 flagged domains against `domain-gateway`'s `REGISTRY` — every one is wired). Fixed the linter to recognize the consolidated-dispatcher pattern (144→109 violations). Also found while checking on task 22: the cron-interval fix it documents as "done 2026-08-26" was never actually committed/pushed — sitting as uncommitted local changes in this clone, `origin/main` still on the old `*/5` schedule. Not pushed yet, flagged to Abdullah.
23. Back on [`22-github-actions-minutes-exhausted.md`](./22-github-actions-minutes-exhausted.md) — 2026-08-27, built option 2: `api/_scenario-drain.ts`, an Edge-safe (20s budget) single-invocation port of `scripts/scenario-worker.mjs`'s drain loop, wired into `misc-gateway`'s REGISTRY, auth'd on the existing `RELAY_SHARED_SECRET`. typecheck/typecheck:api/build/lint all clean. Not runtime-verified (no Redis creds in this dev env) — logic is a direct port of the already-proven CLI worker. Needs you to register the cron-job.org hit (exact config in the task file) before it does anything; nothing in this repo has been pushed yet.
24. [`24-desktop-pro-badge-cleanup.md`](./24-desktop-pro-badge-cleanup.md) — done 2026-08-31, found while re-sweeping `panels.ts` for `premium: 'locked'`: 7 panels + `gpsJamming`/`ciiChoropleth` map layers still carried a `_desktop &&` premium wrapper — task 20's stale-badge pattern, but scoped to the Tauri desktop app so it was never caught. `gpsJamming` was a real functional gate (disabled checkbox), the rest were cosmetic PRO badges. All removed. Flagged, not fixed: 14 pre-existing failing tests across 5 files that still assume `resilienceScore` is `premium: 'locked'` (it hasn't been since task 03) — real test debt, same class as task 14, worth its own task.

Mark a file done by adding `**STATUS: DONE — <date>**` at the top when you finish it. Don't delete the files — they're the record of what shipped and why.

## NOT Abdullah's — stays on Akul's plate

These still need a decision only Akul can make. Don't pick them up until they move to the queue above with a decision already attached.

- **[`05-supply-chain-chokepoints.md`](./05-supply-chain-chokepoints.md)** — the locked analytics this file originally referred to (8 supply-chain RPCs) were unlocked in task 12 (confirmed not Railway-dependent) and scenario modeling was unlocked in task 15 (GitHub Actions instead of Railway). Nothing left blocked on this file specifically.
- **Railway seed-infra go/no-go** — still deferred (cost/billing risk). Blocks `news/v1/list-feed-digest` (partially — only the subset of feeds IP-blocked from Vercel), `telegram-feed` (fully, hard 503 without it), `gpsjam` (silently stale without the seed cron), `oref-alerts` (degrades to empty/stale). Scenario modeling and the 3 panels below are OFF this list now (see 10/12/15/16/17/18/19/20) — this is only the 4 endpoints above plus `convex/config/productCatalog.ts`'s BLS-series seed (`server/worldmonitor/economic/v1/get-bls-series.ts`), found during task 12's investigation.
- **`latest-brief`** — the one remaining genuinely-locked panel. Confirmed structurally, not by inference (task 15's investigation traced the full chain): real Dodo subscription → real Convex entitlement row → Convex-gated alert rule → the deferred Railway digest cron → Redis `brief:*` keys this endpoint just reads. No free/anonymous-compatible alt-source exists — every link needs real WorldMonitor SaaS infrastructure this fork doesn't have. Not fixable by unlocking a gate.
